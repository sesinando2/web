class PhoneInputController {

  /* @ngInject */
  constructor(formatService, validationService) {
    this.formatService = formatService;
    this.validationService = validationService;
  }

  $onInit() {

  }
}

export default PhoneInputController;
